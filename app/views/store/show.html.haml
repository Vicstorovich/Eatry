%p= link_to 'Welcome', root_path

%h1 Store#show



- @categories.each do |category|
  %strong
    = category.title
  - category.products.menu_products(@menu).each do |product|
    .entry
      = image_tag(product.image_url)
      %h3= product.title
      |
      = sanitize(product.description)
      .price_line
        %span.price= sprintf("$%0.02f", product.price)
